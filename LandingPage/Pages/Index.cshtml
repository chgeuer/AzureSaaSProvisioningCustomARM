@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <p>See the <a href="@(Model.GetAddress(Model.Config.ARM.TemplateName))">@Model.Config.ARM.TemplateName</a></p>
    <p>See the <a href="@(Model.GetAddress(Model.Config.ARM.UIDefinitionName))">@Model.Config.ARM.UIDefinitionName</a></p>
    <p>See the <a href="@(Model.GetAddress("README.md"))">README.md</a></p>

    <p>
        <a href="@Model.DeploymentURL">
            <img src="~/images/deploytoazure.svg" alt="Deploy to Azure" />
        </a>
    </p>
</div>